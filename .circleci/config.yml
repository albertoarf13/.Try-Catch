version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:16.14

    steps:
      - checkout
      - run: npm install
      - run:
          name: Save test results
          command: |
            mkdir -p ~/test-results
            npm test > ~/test-results/results.txt
            pwd
          when: always
      - store_test_results:
          path: ~/test-results